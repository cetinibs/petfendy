# ============================================================================
# PETFENDY ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control!
#
# PRD Compliance: All secrets must be server-side only
# DO NOT use NEXT_PUBLIC_ prefix for secrets!
# ============================================================================

# ----------------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# ----------------------------------------------------------------------------

# JWT Secrets (CRITICAL - Generate strong random values)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-this
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-change-this

# Encryption Key (CRITICAL - Generate strong random value)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-super-secret-encryption-key-min-32-chars-change-this

# Secret Rotation (Optional - Set when secrets were last rotated)
# Format: YYYY-MM-DD
SECRETS_LAST_ROTATED=2025-11-11

# ----------------------------------------------------------------------------
# PAYMENT GATEWAY
# ----------------------------------------------------------------------------

# Payment Provider Configuration (Choose one: iyzico, paytr, stripe)
PAYMENT_PROVIDER=iyzico
PAYMENT_API_KEY=your-payment-api-key-here
PAYMENT_MERCHANT_ID=your-merchant-id-here
PAYMENT_API_URL=https://api.payment-gateway.com

# Stripe (if using Stripe)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_PUBLISHABLE_KEY=pk_live_...

# İyzico (if using İyzico)
# IYZICO_API_KEY=your-iyzico-api-key
# IYZICO_SECRET_KEY=your-iyzico-secret-key
# IYZICO_BASE_URL=https://api.iyzipay.com

# PayTR (if using PayTR)
# PAYTR_MERCHANT_ID=your-merchant-id
# PAYTR_MERCHANT_KEY=your-merchant-key
# PAYTR_MERCHANT_SALT=your-merchant-salt

# ----------------------------------------------------------------------------
# EMAIL SERVICE
# ----------------------------------------------------------------------------

# SendGrid Configuration
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here
EMAIL_FROM=noreply@petfendy.com
EMAIL_FROM_NAME=Petfendy Pet Hotel

# Alternative: AWS SES
# AWS_SES_REGION=eu-west-1
# AWS_SES_ACCESS_KEY=your-access-key
# AWS_SES_SECRET_KEY=your-secret-key

# ----------------------------------------------------------------------------
# DATABASE
# ----------------------------------------------------------------------------

# PostgreSQL Database URL
DATABASE_URL=postgresql://user:password@localhost:5432/petfendy

# Alternative: MongoDB
# MONGODB_URI=mongodb://localhost:27017/petfendy

# Database Pool Configuration
DB_POOL_MIN=2
DB_POOL_MAX=10

# ----------------------------------------------------------------------------
# SECURITY CONFIGURATION
# ----------------------------------------------------------------------------

# Billing Limits (in USD)
DAILY_BILLING_LIMIT=10
MONTHLY_BILLING_LIMIT=100

# Rate Limiting
RATE_LIMIT_PER_MINUTE=30
RATE_LIMIT_PER_HOUR=1000

# Payload Limits
MAX_PAYLOAD_SIZE=204800
MAX_TOKENS=2000

# Session Configuration
SESSION_TIMEOUT=86400
REFRESH_TOKEN_EXPIRY=604800

# ----------------------------------------------------------------------------
# STORAGE & CDN
# ----------------------------------------------------------------------------

# AWS S3 (for file uploads)
AWS_S3_BUCKET=petfendy-uploads
AWS_S3_REGION=eu-west-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Cloudflare R2 (alternative)
# R2_ACCOUNT_ID=your-account-id
# R2_ACCESS_KEY_ID=your-access-key
# R2_SECRET_ACCESS_KEY=your-secret-key
# R2_BUCKET_NAME=petfendy-uploads

# ----------------------------------------------------------------------------
# SMS PROVIDER
# ----------------------------------------------------------------------------

# Twilio Configuration
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Alternative: Vonage
# VONAGE_API_KEY=your-vonage-api-key
# VONAGE_API_SECRET=your-vonage-api-secret

# ----------------------------------------------------------------------------
# ANALYTICS & MONITORING
# ----------------------------------------------------------------------------

# Vercel Analytics
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=your-analytics-id

# Google Analytics
# NEXT_PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX

# Sentry (Error Tracking)
# SENTRY_DSN=https://your-sentry-dsn
# SENTRY_AUTH_TOKEN=your-sentry-auth-token

# ----------------------------------------------------------------------------
# DEVELOPMENT CONFIGURATION
# ----------------------------------------------------------------------------

# Node Environment
NODE_ENV=development

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Debug Mode
DEBUG=false

# ----------------------------------------------------------------------------
# FEATURE FLAGS
# ----------------------------------------------------------------------------

# Enable/Disable Features
ENABLE_PAYMENT_GATEWAY=false
ENABLE_EMAIL_NOTIFICATIONS=false
ENABLE_SMS_NOTIFICATIONS=false
ENABLE_ANALYTICS=false

# ----------------------------------------------------------------------------
# LOCALIZATION
# ----------------------------------------------------------------------------

# Default Locale
NEXT_PUBLIC_DEFAULT_LOCALE=tr
NEXT_PUBLIC_SUPPORTED_LOCALES=tr,en

# ----------------------------------------------------------------------------
# REDIS (for production rate limiting & caching)
# ----------------------------------------------------------------------------

# Redis URL
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=your-redis-password

# ----------------------------------------------------------------------------
# SECURITY HEADERS
# ----------------------------------------------------------------------------

# Content Security Policy
CSP_REPORT_URI=https://your-csp-report-endpoint

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# 1. Generate strong secrets:
#    - JWT_SECRET: openssl rand -hex 32
#    - ENCRYPTION_KEY: openssl rand -hex 32
#
# 2. NEVER use NEXT_PUBLIC_ prefix for sensitive values
#    - NEXT_PUBLIC_ exposes variables to the browser
#    - Only use for non-sensitive configuration
#
# 3. Rotate secrets every 30-60 days:
#    - Update SECRETS_LAST_ROTATED when rotating
#
# 4. Keep this file (.env.example) in version control
#    - DO NOT put real values here
#    - This is just a template
#
# 5. Production deployment:
#    - Set these in your hosting provider's dashboard
#    - Vercel: Project Settings > Environment Variables
#    - AWS: Parameter Store or Secrets Manager
#    - Docker: Use .env file (gitignored) or docker secrets
# ============================================================================
